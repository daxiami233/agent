请根据上传的截图，修复当前 Agent 前端展示和运行逻辑问题。

1. 修复 Markdown / 公式渲染

当前公式没有正确渲染，请检查消息渲染组件，确保支持：
- Markdown 基础语法；
- 行内代码、代码块；
- 行内公式、块级公式；
- 列表、加粗、斜体。

2. 修复“生成已停止”提示显示

当前“检测到重复工具调用，已停止继续调用”提示和上方输出混在一起。

要求：
- 不需要单独气泡；
- 继续显示在同一个 assistant 输出区域；
- 但要和上方内容空一行；
- 避免直接紧贴上一段正文。

3. 修复多行输入布局

当前输入框输入多行后，思考按钮会跟着向上跑。

要求：
- 输入框高度变化时，按钮区域位置保持稳定；
- 思考按钮、发送按钮固定对齐在输入区底部或固定位置。

4. 修复上下文窗口显示

新建对话时，上下文窗口使用量应为空或 0。

要求：
- 新对话刚创建时不要显示不准确的上下文占用；
- 只有完成一轮对话后，才显示真实上下文使用量；
- 后续每轮对话完成后再更新。

5. 增加上下文压缩提示

触发上下文压缩时，在对话中显示状态：

- 开始压缩：正在压缩上下文...
- 压缩完成：上下文压缩完成

要求：
- 不要混入 assistant 正文；
- 可以作为系统状态行显示；
- 用户需要明确知道当前正在压缩或已经压缩完成。

6. 优化重复工具调用控制

当前连续相同参数调用同一工具会直接停止，请改成两级策略。

第一次检测到重复调用：
- 不立即停止；
- 向下一次模型调用上下文加入提醒；
- 提醒用于约束模型，不一定需要作为前端消息展示。

建议加入模型上下文的英文提醒：

Duplicate call detected for {tool_name} with the same arguments. Do not call it again. Continue using the existing result.

提醒后仍然重复调用：
- 临时屏蔽该工具 1 轮；
- 屏蔽期间不要把该工具加入模型可用工具列表；
- 下一轮自动恢复；
- 不需要额外前端提示。

7. 优化项目 system prompt 中空 tools / skills 的英文表述

项目生成 system prompt 时，不要直接显示 None、null、undefined。

请在项目 system prompt 中使用英文自然语言描述空状态。

当用户 tools 为空但系统 tools 可用时，使用：

No user-defined tools are loaded. Built-in system tools are still available.

当用户 skills 为空但系统 skills 可用时，使用：

No user-defined skills are loaded. Built-in system skills are still available.

当完全没有工具时，使用：

No tools are currently available.

当完全没有 Skill 时，使用：

No skills are currently available.

要求：
- 这些文案是项目内部 system prompt 中使用的内容；
- 不要让 system prompt 出现 None、null、undefined；
- 要区分“没有用户自定义项”和“完全没有可用项”。

8. 新增系统内置工具 list_skills

新增一个默认加载的系统工具：

tool name: list_skills

功能：
列出当前所有可用 Skill，包括系统内置 Skill 和用户自定义 Skill。

返回格式：

[
  {
    "name": "xxx",
    "description": "xxx"
  }
]

要求：
- 不需要返回 source 字段；
- 返回结构化数组；
- 无可用 Skill 时返回空数组，不要返回 None；
- 方便模型根据 Skill 名称和描述选择是否调用。

9. 隔离系统 skills 和用户 skills

系统默认目录：

系统 Skill：
/Users/xiachangwei/Desktop/agent/src/agent_runtime/skills


要求：
- 系统 skills 默认加载；
- 用户传入的 skill 目录额外加载；
- 系统目录和用户目录保持隔离；
- 加载日志中区分 system / user 来源；
- 如果 tool / skill 名称冲突，不要静默覆盖，直接报错并提示冲突。

10. 新增系统内置示例 Skill，并整理系统目录结构

删除当前的 `examples.py`，不再使用单文件集中定义示例 Skill。后续每个 Skill 都应该采用独立文件夹形式组织，便于扩展、加载和维护。

请重新整理以下两个系统目录：

```text
/Users/xiachangwei/Desktop/agent/src/agent_runtime/skills
/Users/xiachangwei/Desktop/agent/src/agent_runtime/tools
```

目录结构要求：

```text
skills/
  configs/        # 存放系统 Skill 的配置文件
  builtin/        # 存放系统内置 Skill，每个 Skill 一个独立文件夹

tools/
  configs/        # 存放系统 Tool 的配置文件
  builtin/        # 存放系统内置 Tool，每个 Tool 一个独立文件夹
```

要求：

* 删除旧的 `examples.py`；
* 每个 Skill 使用独立文件夹组织；
* 每个 Tool 也使用独立文件夹组织；
* 配置文件和具体实现分离；
* 系统内置 Skill / Tool 默认从对应目录加载；
* 保持后续扩展时目录结构清晰一致。

在系统 Skill 目录中新增一个默认 Skill：

```text
name: demo_echo_skill
```

```text
description:
A demo skill used to verify built-in skill loading, discovery, and invocation. It accepts user text and returns a structured echo result.
```

要求：

* `demo_echo_skill` 作为系统内置 Skill 默认加载；
* 能被 `list_skills` 查询到；
* 不依赖外部服务；
* 用于验证系统 Skill 的加载、发现和调用流程。

